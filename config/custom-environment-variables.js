const config = {
  elasticSearch: {
    host: 'HOST',
    httpAuth: "HTTP_AUTH",
    requestTimeout: "REQUEST_TIMEOUT"
  },
  hdfs: {
    user: 'USER',
    host: 'HOST',
    port: 'PORT'
  }   
};

module.exports = config;